# The US National Weather Service requires a contact (an email address or URL) in
# the User-Agent of every request and rejects calls that omit it. Copy this file to
# .env and set your own. .env is gitignored so your address never gets committed.
NWS_CONTACT=you@example.com
